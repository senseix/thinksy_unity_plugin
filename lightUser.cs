using System;

	//light weight user info stored in link list or array
	//array or list gets flushed and filled with new nodes when leaderboard gets updated
	public class lightUser: user
	{

		public lightUser ()
		{
			//Just keep the default value
		}
		public lightUser (string id,string name,int rank,int score)
		{
			//There is not valid checking, make sure this function properly called
		}
	}
